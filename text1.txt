{
    git install in pc
    cmd ---- git config --global user.name "omprasathop"
             git config --global user.email "omprassathop@gmail.com"
} 
{
    git to vscode ---- in terminal ----- git clone Git_Folder_Name_code
}
{
    vscode new file to add in the git local ----- git add File_Name
                         modified file      ----- git commit -m "Write-your-any-comments"
}
{
    vscode new file to add in the git online ----- git push origin main
}
{
    to create new file in git access in the vscode ---- git pull 
}
{
    create a new folder in pc to add in git --- to create repository in git--
    ---- git init
    ---- git remote add origin https:code
    ---- git branch
    change branch name ---- git branch -M main
    --- git add .
    --- git commit -m "create a files in pc"
    --- git push origin main
}

branch
{
    create another branch *main *sub-branch
    ---- git pull
    ---- git branch -a --- it's show all branch
    ---- git checkout sun-branch ----  change the main to sub-branch
    ---- git add .
    ---- git commit -m "any"
    ---- git push origin sub-branch
    ---- git checkout main
    ---- git diff sub-branch --- the diiferent betweem two branch
    ---- git merge sub-branch   ---- to join the two branch in main branch
    ---- git push origin main
}